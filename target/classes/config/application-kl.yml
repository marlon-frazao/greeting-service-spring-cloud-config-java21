server:
  port: 8080
spring:
  application:
    name: greeting-service
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"
  endpoint:
    health:
      show-details: always
greeting-service:
  greeting: "nuqneH"
  default-value: "Qo'noS"
